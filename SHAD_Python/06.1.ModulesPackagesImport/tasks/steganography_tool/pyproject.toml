[build-system]
requires = ["setuptools >= 61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "steganography_tool"
version = "0.1"
description = "simple LSB"
authors = [
    {name = "MVenjoyer", email = "ggg.gggyy@yandex.ru"}
]

dependencies = [
    "click",
    "pillow",
    "numpy"
]

[project.scripts]
steganography-tool = "steganography_tool.cli:cli"
